{{- if .Values.search_engine_app.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "..fullname" . }}
  labels:
    {{- include "..labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "..fullname" . }}
  minReplicas: {{ .Values.search_engine_app.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.search_engine_app.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.search_engine_app.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.search_engine_app.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.search_engine_app.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.search_engine_app.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
