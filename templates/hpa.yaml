{{- if .Values.searchengineapp.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "..fullname" . }}
  labels:
    {{- include "..labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "..fullname" . }}
  minReplicas: {{ .Values.searchengineapp.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.searchengineapp.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.searchengineapp.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.searchengineapp.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.searchengineapp.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.searchengineapp.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
