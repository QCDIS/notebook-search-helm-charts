apiVersion: v1
kind: Service
metadata:
  name: {{ include "deleteme.fullname" . }}
  labels:
    {{- include "deleteme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deleteme.selectorLabels" . | nindent 4 }}
